variables:
  KUBE_NAMESPACE: certisign
  HELM_RELEASE_NAME: iframe

include:
  - project: "inovacao/devops/template-cicd"
    file: "build-svelte.yaml"
  - project: "inovacao/devops/template-cicd"
    file: "deploy.yaml"

build:
  extends: .build

stages:
  - build
  - deploy
